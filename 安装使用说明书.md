# 财务管理台 · 安装使用说明书

版本 1.1.0

---

## 这是什么

一套给项目型企业用的财务台账系统，管六本账：

| 模块 | 管什么 |
|---|---|
| 设施摊销 | 设施按月摊销，支持从旧账接期初累计 |
| 专项费用 | 按累计产值和比例计提，逐期串成链条 |
| 工会经费 | 按工资总额计提，含上解与留存 |
| 固定资产折旧 | 按月折旧，支持期初已折旧接续 |
| 坏账准备 | 按账龄计提减值 |
| 低值易耗品 | 领用摊销 |

装在一台服务器上，同事各自用浏览器访问，看到的是同一套账。有账号权限、结账锁定、审计日志和自动备份。

**技术上只有一个要求：Node.js 22.5 或以上。** 没有任何第三方依赖，不需要 `npm install`，不需要数据库服务——数据就是一个 SQLite 文件。

---

## 发布件里有什么

```
fin-console-1.1.0.tar.gz     Linux / WSL / Docker 用
fin-console-1.1.0.zip        Windows Server 用
SHA256SUMS.txt               校验和
```

两个包内容完全相同，只是压缩格式不同。解开后是一个 `fin-console-1.1.0/` 目录：

```
fin-console-1.1.0/
├── server.js              入口
├── lib/                   服务端模块（14 个）
├── frontend/              前端源码（原生 ES 模块，不打包）
├── public/                前端构建产物（可直接用，也可重新构建）
├── scripts/               构建、自检、备份、打包等工具
├── deploy/                安装脚本、systemd 单元、nginx 模板
├── Dockerfile
├── docker-compose.yml
├── config.example.json
├── package.json
└── README.md              开发者向的详细文档
```

拷到目标机器后先验一下包没坏：

```bash
sha256sum -c SHA256SUMS.txt          # Linux
certutil -hashfile fin-console-1.1.0.zip SHA256    # Windows，跟 SHA256SUMS.txt 里的值比对
```

---

## 装之前先确认环境

```bash
node -v                              # 必须 >= v22.5
node -e "require('node:sqlite')"     # 无报错即可
```

Node 22.5 是硬要求：系统用的是 Node 自带的 `node:sqlite`，这个模块在 22.5 才有。版本不够会直接启动失败，不会带着隐患跑起来。

没装 Node 的话：

- Linux：https://github.com/nodesource/distributions
- Windows：https://nodejs.org/ 下 22 LTS 的 msi，一路下一步

---

## 四种装法，选一种

### 方法一：先在自己电脑上试试（最快）

不用装服务，起来就能看，关掉就没了。适合先看看合不合用。

```bash
tar -xzf fin-console-1.1.0.tar.gz
cd fin-console-1.1.0
node scripts/demo.mjs
```

它会起一个演示实例，预置好 3 个单位、5 个项目、3 月已录完并结账的数据，终端会打印地址和账号口令。想推平重来加 `--reset`。

演示数据放在 `data-demo/`，删掉目录就干净了。

### 方法二：Linux 服务器（推荐用于正式使用）

```bash
tar -xzf fin-console-1.1.0.tar.gz
cd fin-console-1.1.0
sudo bash deploy/install-linux.sh
```

脚本会做这些事，每一步失败都会停下来告诉你原因：

1. 检查 Node 版本和 `node:sqlite`
2. 建一个不能登录的系统账号 `finconsole` 跑服务（不用 root）
3. 程序装到 `/opt/fin-console`，数据放 `/var/lib/fin-console`
4. 构建前端并做两道校验
5. 配置写到 `/etc/fin-console.env`，权限强制 `root:root 0600`（里面可能有初始口令）
6. 注册 systemd 服务、开机自启
7. **轮询 `/api/health` 直到真的起来**才报成功

重复执行就是升级，账套数据不动。

装完看初始管理员口令：

```bash
sudo journalctl -u fin-console -n 40 --no-pager
```

常用命令：

```bash
sudo systemctl status fin-console        # 看状态
sudo systemctl restart fin-console       # 重启
sudo journalctl -u fin-console -f        # 跟踪日志
```

### 方法三：Docker

```bash
tar -xzf fin-console-1.1.0.tar.gz
cd fin-console-1.1.0
docker compose up -d --build
docker compose logs | grep 口令          # 看初始管理员口令
```

数据在名为 `fin-data` 的 volume 里，换服务器时整卷搬走。

`docker-compose.yml` 里 `FIN_HOST` 是 `0.0.0.0` —— 这是对的，容器内的回环宿主机访问不到。对外的收口靠 `ports` 那行 `"127.0.0.1:8787:8787"`，端口只映射到宿主回环，局域网仍然连不上。

### 方法四：Windows Server

解压 zip，进到 `fin-console-1.1.0\deploy\`，右键 `install-windows.bat` → **以管理员身份运行**。

装了 [nssm](https://nssm.cc/) 会注册成正规 Windows 服务；没装则退回「计划任务开机自启」。两种都能用，nssm 的日志和重启策略更好。

数据放在 `C:\ProgramData\FinConsole`，初始口令在 `C:\ProgramData\FinConsole\service.log`。

---

## 第一次登录要做的四件事

### 1. 改掉初始口令

打开 `http://服务器地址:8787`，用日志里的账号口令登录。系统会**强制**你改。

这不是走个形式：**改之前服务端拒绝一切业务操作** —— 录数据、导出全部返回 403。因为初始口令是打印在日志、写在部署脚本里的，暴露面比看起来大。

### 2. 给同事开账号

「⋯ → 账号管理」。三种角色：

| 角色 | 能做什么 |
|---|---|
| 管理员 | 全部权限，含账号管理、备份、恢复、导出整套账、维护清单 |
| 记账员 | 录入、修改、删除、结账、重开、本月结转、粘贴导入 |
| 只读 | 只能查看、打印、导出 Excel、看操作日志 |

系统自动生成初始口令，**线下告知本人**，他首次登录也会被强制改。

管理员至少留两个。系统不允许最后一个管理员把自己降级或停用（管理权限归零后谁都改不回来，只能上手改数据库），但两个人互相有个备份总是更稳。

### 3. 建单位和项目清单（重要，最好在录数据之前）

「⋯ → 单位/项目清单」。

**为什么这一步不能省**：专项费用按「单位 + 项目名称」把各期串成一条链，用上期累计推算本期产值；工会经费按「单位」串。名称差一个字——多个空格、全角括号写成半角、「一分公司」写成「1分公司」——链条就断成两条，上期基数变成 0，本期产值被当成全部累计额，**计提金额凭空翻倍，而且不会有任何报错**。

清单建起来之后：

- 录入界面的「单位」「项目名称」自动变成下拉框，服务端也开始拒绝清单外的名称
- 项目可以登记计提比例，选中项目自动带出（手改后不会被覆盖）
- 清单**为空时不强制**，新装的系统可以直接开始录（否则会被自己堵在门外）
- 已经录了数据才想起要建：点「从既有台账归集」，扫描台账里出现过的名称一次性收进清单

**改名只能走「改名」按钮**：它在一个事务里同时改清单和所有历史台账，链条保持完整。新建一个正确的名字不等于改名——历史会留在旧名下，链条照样断。

项目完工请「停用」而不是删除：停用后不再进入结转名册，历史数据完整保留。

### 4. 设一下编制单位名称

打印表头上的公司全称。Linux 改 `/etc/fin-console.env` 里的 `FIN_ORG_NAME`，Windows 改 `config.json` 里的 `orgName`，改完重启服务。

---

## 两个「期间」的区别（容易混，先看这个）

界面顶栏有两样东西：

| | 查看期间 | 账套期间 |
|---|---|---|
| 是什么 | 我现在想看哪个月 | 这套账推进到哪个月 |
| 范围 | **只影响我自己** | 全公司一个值 |
| 谁能改 | 任何人，包括只读账号 | 需要结账权限（管理员 / 记账员） |
| 改了会怎样 | 我的筛选、报表口径、新增记录预填的期间跟着变 | 设施摊销 / 固定资产折旧 / 坏账准备的新记录归属期跟着变，从而决定结账锁住哪些数据 |
| 怎么改 | 顶栏月份选择器、「上月 / 下月」 | 顶栏「推进账套期间」按钮（先把查看期间切到目标月） |

想翻回上个月核对一下，直接改查看期间，**不会影响正在录本月数据的同事**。只有真要宣布「这套账进入下个月」时才推进账套期间。

没有结账权限的账号看不到「推进账套期间」按钮——摆出来只会让人反复点然后收一串权限错误。

**结账始终是全局的**：按「模块 × 期间」锁定，谁在看哪个月都不影响锁的事实。

---

## 每个月怎么走

1. 顶栏把**查看期间**切到本月
2. 「⋯ → 本月结转」：选来源期间（默认上一期）→ 看一遍名册 → 填这期的新数字（可以留空之后补）→ 确认
   - 上期备注写了「完工 / 已结束 / 停工」的自动跳过
   - 本期已有的记录不会重复生成
   - 填的开累数小于上期会被拒绝（那说明数字填错了）
3. 数字多的时候用「⋯ → Excel 粘贴导入」：从 Excel 选一块区域贴进来 → 「校验并预览」→ 看清哪几行有问题 → 只导入通过校验的行
4. 设施摊销和固定资产折旧不用按月录入，切期间就是当期数
5. 核对无误后本模块结账，再把结果录进正式核算系统

### 从旧账接过来

不用逐月补录历史。设施填「期初已摊销（元）」+「期初截止期间」，固定资产填「期初已折旧（元）」+「期初截止期间」，**两项必须成对填写**。填了之后系统从这个基数按月往下摊，不再从启用日期重算。

专项费用和工会经费对应「上期累计产值（元）」「期初已计提金额（元）」这类字段，只对最早一期生效。

---

## 备份与恢复

服务端每 24 小时自动热备一份到数据目录的 `backups/`，保留 30 份。也可以随时手动：

- 界面：「⋯ → 数据备份 → 立即备份」
- 命令行：`node scripts/backup-now.mjs`（适合挂 cron）
- 整套账 JSON：「⋯ → 下载整套账 JSON」（含台账、受控清单、结账锁、共享设置）

### 恢复方式一：从 JSON 恢复（不用停服）

「⋯ → 数据备份 → 从 JSON 恢复」，选之前导出的 JSON。两步：

1. **先预演**。服务端只解析不写库，把逐模块的「现有 N 条 → 恢复为 M 条」摆给你看，并列出受控清单、结账记录的数量，以及无法解析的内容。
2. 看清了再输入确认词 `替换全部数据` 才真正执行。服务端在覆盖前会自动热备一次现状，万一恢复错了还有退路。

这是**整库替换**，不是合并。合并要逐条判断「哪两条是同一笔业务」，而这套数据的天然主键是「单位｜项目｜期间」这类业务组合键，一旦判错就会静默产生重复计提。所以宁可要求整体覆盖。

### 恢复方式二：直接换库文件（需要停服）

```bash
sudo systemctl stop fin-console
cd /var/lib/fin-console
cp backups/fin-20260315-030000.db fin.db
rm -f fin.db-wal fin.db-shm          # 必须删掉，否则会用旧的预写日志覆盖
sudo systemctl start fin-console
```

### 忘记口令

```bash
cd /opt/fin-console/server
sudo -u finconsole node scripts/reset-password.mjs --list      # 看有哪些账号
sudo -u finconsole node scripts/reset-password.mjs admin       # 交互式输入
```

口令走交互式隐藏输入，**不要写在命令行参数里** —— 那会留在 shell history 和进程列表里。自动化场景可以从管道读：`echo '新口令Ab123456' | node scripts/reset-password.mjs admin`。

重置后该账号所有会话立即失效，下次登录还要再改一次。

---

## 安全须知

### 已经做了的

- 口令 scrypt 加盐哈希，HttpOnly + SameSite=Strict 会话 Cookie
- 首登强制改口令**由服务端强制**：未改之前只放行改口令与查看自身信息
- 登录限流按三个维度分别计数（`用户名@IP`、纯 IP、纯用户名），换用户名或伪造 `X-Forwarded-For` 都绕不过
- 最后一个管理员不能自我降级或被停用
- 所有文本输出 HTML 转义，备注里的 `<img src=x onerror=...>` 不会被执行
- 服务端字段白名单校验，绕过前端也写不进脏数据
- 静态文件路径穿越防护、跨站写请求拦截、CSP 响应头
- 健康检查默认不含业务数据（`/api/health` 无需登录，端口误暴露时不该交出账套规模）
- 非 root 运行，systemd 文件系统加固

### 还需要你决定的

**1. 默认只监听 `127.0.0.1`。** 也就是只有服务器本机能访问。要让同事在局域网用，改 `FIN_HOST=0.0.0.0` 并放行防火墙：

```bash
# Linux
sudo ufw allow from 192.168.1.0/24 to any port 8787

# Windows
netsh advfirewall firewall add rule name="财务管理台" dir=in action=allow protocol=TCP localport=8787
```

**只放行需要的网段**，不要对整个网络开放。

**2. 强烈建议挂 HTTPS。** 裸 HTTP 下口令和财务数据在网络上是明文。用 `deploy/nginx-fin-console.conf` 做模板，配好之后把这两项打开：

```
FIN_COOKIE_SECURE=1     # 会话 Cookie 带 Secure 标记
FIN_TRUST_PROXY=1       # 日志记录真实来访 IP 而不是代理的
```

顺序很重要：**先配好 HTTPS 再开这两项**。反过来会导致 Cookie 在 HTTP 下发不出去，谁都登不上。

**3. 不要直接暴露到公网。** 真需要外网办公，走 VPN 或在反向代理上做 IP 白名单（示例配置里已经写了内网段限制）。

---

## 常见问题

**服务在跑，但浏览器打不开 `localhost:8787`**

先分清是哪一种：

```bash
curl http://127.0.0.1:8787/api/health      # 通不通？
```

- 这个不通 → 服务真的没起来，看日志
- 这个通但浏览器打不开 → 浏览器解析 `localhost` 优先走 IPv6 的 `::1`。系统在监听 `127.0.0.1` 时会自动补一个 `::1` 监听，所以两个都应该通；如果 `::1` 不通，直接用 `http://127.0.0.1:8787` 就行
- 都通但同事的电脑打不开 → 那是 `FIN_HOST=127.0.0.1` 的正常表现，见上面「安全须知」第 1 条

**改了前端之后页面没变化**

```bash
cd /opt/fin-console/server
sudo -u finconsole node scripts/build-frontend.mjs
sudo systemctl restart fin-console
```

前端源码在 `frontend/`，`public/` 是构建产物。**别直接改 `public/`** —— 下次构建会覆盖掉。

浏览器刷新即生效（HTML 不缓存，静态资源用 ETag 校验）。

**提示「期间已结账」改不了数据**

正常。要改就先「重开」该期间（需要结账权限），改完再结账。重开和结账都会写审计日志。

注意设施摊销、固定资产折旧、坏账准备这三个模块没有期间字段，它们判锁看的是**账套期间**不是你在看的月份。

**两个人同时改一条记录会怎样**

后保存的人会收到提示并自动刷成最新值，**不会静默覆盖**前一个人的修改。这是有意设计的——共享账套如果允许静默覆盖，改丢了数据谁都不知道。

**数据库文件能直接拷走吗**

能，但要先停服，而且必须连 `fin.db-wal`、`fin.db-shm` 一起拷（或者停服后它们已经合并进主库）。不停服拷贝可能拿到不一致的快照。更稳的做法是用「立即备份」——它走的是 SQLite 的在线备份接口，不用停服。

**WSL 里跑，重启后服务没了**

WSL 默认没有 systemd。要么每次手动起，要么在 `/etc/wsl.conf` 里加 `[boot]` `systemd=true` 后重启 WSL，再用 `install-linux.sh` 装成服务。

---

## 自检

改过代码或者想确认部署没问题：

```bash
cd fin-console-1.1.0
npm run verify           # 模块图 + 构建产物校验
npm run test:frontend    # 前端核心逻辑（17 项，不需要浏览器）
npm test                 # 接口端到端（115 项，临时库跑完即删）
npm run test:all         # 全跑
```

`npm test` 会自己起一个临时实例、用临时数据目录，跑完删掉，**不会碰到你的正式账套**。

`npm run test:browser` 需要本机装 Edge/Chrome，会真的打开页面走完整流程。没装的话 `test:all` 会明确报「跳过」而不是「通过」。

---

## 数据放在哪

| 部署方式 | 程序 | 数据 | 配置 |
|---|---|---|---|
| Linux + systemd | `/opt/fin-console/server` | `/var/lib/fin-console` | `/etc/fin-console.env` |
| Docker | 容器内 `/app` | volume `fin-data` | `docker-compose.yml` 的 environment |
| Windows | 解包目录 | `C:\ProgramData\FinConsole` | `config.json` |

数据目录里：

```
fin.db              主库（全部账套数据）
fin.db-wal          预写日志（运行时存在）
fin.db-shm          共享内存索引（运行时存在）
backups/            自动热备
```

**换服务器只需要搬走数据目录**，程序重新装一遍就行。

---

## 配置项速查

环境变量优先，其次同目录的 `config.json`。

| 变量 | 默认 | 说明 |
|---|---|---|
| `FIN_HOST` | `127.0.0.1` | 监听地址。`0.0.0.0` 才能让局域网访问 |
| `FIN_PORT` | `8787` | 端口 |
| `FIN_DATA_DIR` | `data` | 数据目录 |
| `FIN_ORG_NAME` | 空 | 打印表头「编制单位」 |
| `FIN_ADMIN_USER` | `admin` | 首次启动创建的管理员账号 |
| `FIN_ADMIN_PASSWORD` | 空 | 留空则自动生成并打印到日志（**更安全，推荐留空**） |
| `FIN_COOKIE_SECURE` | `0` | 挂 HTTPS 后设 1 |
| `FIN_TRUST_PROXY` | `0` | 在反向代理后设 1，日志才记真实 IP |
| `FIN_SESSION_HOURS` | `12` | 会话有效期 |
| `FIN_LOGIN_MAX_ATTEMPTS` | `8` | 登录失败次数上限 |
| `FIN_LOGIN_WINDOW_MINUTES` | `15` | 限流时间窗 |
| `FIN_BACKUP_INTERVAL_HOURS` | `24` | 自动热备间隔（0 关闭） |
| `FIN_BACKUP_KEEP` | `30` | 保留份数 |
| `FIN_COMPRESS` | `1` | gzip 压缩。前面 nginx 已压缩可设 0 |
| `FIN_HEALTH_DETAIL` | `0` | 健康检查是否对匿名请求返回记录数 |
| `FIN_MAX_ROWS` | `20000` | 单模块单次返回上限 |
| `TZ` | 系统 | 建议 `Asia/Shanghai` |

---

## 升级

1. 先备份：界面「立即备份」，或者 `node scripts/backup-now.mjs`
2. 解开新版发布件
3. 重跑安装脚本（`install-linux.sh` / `install-windows.bat` 都是幂等的，会保留数据和配置）
4. 打开界面确认能登录、数据都在

Docker：`docker compose up -d --build` 即可，数据在 volume 里不受影响。

---

## 重新打包

改过代码想自己出一份发布件：

```bash
node scripts/pack.mjs              # 用 package.json 里的版本号
node scripts/pack.mjs 1.0.1        # 指定版本号
```

产物在 `dist/`，同时生成 `SHA256SUMS.txt`。打包前会强制重新构建前端，不会把过期产物发出去；`data/`、`config.json`、日志、`.git` 都排除在外，发布件里不会带任何一台具体机器的痕迹。

同样的源码打出来的包字节一致（时间戳和 uid/gid 都固定），方便核对。
