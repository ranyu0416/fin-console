# 财务管理台 · 云端部署与二次开发手册

版本：1.1.0

> 本手册面向需要把财务管理台部署到云服务器，并且后续自行增加模块、调整字段或升级版本的人员。
> 正式部署前请同时阅读《安装使用说明书.md》；本手册重点补充云服务器和开发维护部分。

---

## 1. 系统边界

这套系统是“计提台账 + 辅助核算 + 留痕”工具，不替代正式财务核算系统。系统负责录入、跨期计算、结转、结账、审计和备份；核对无误后，结果仍需按公司的正式流程进入正式核算系统。

当前模块：

| key | 模块 | 是否按月记录 | 主要口径 |
|---|---|---:|---|
| `facility` | 设施摊销 | 否 | 按账套期间计算 |
| `levy` | 专项费用 | 是 | 累计产值差额 × 比例 |
| `union` | 工会·职工教育经费 | 是 | 工资累计口径 |
| `asset` | 固定资产折旧 | 否 | 按账套期间计算 |
| `baddebt` | 减值准备 | 否 | 按账套期间计算 |
| `lvc` | 低值易耗品 | 是 | 本期领用数量、摊销 |

系统使用 Node.js 内置 `node:sqlite`，不需要 MySQL、PostgreSQL 或 Redis。Node.js 版本必须为 22.5 或更高。

### 最重要的原则

**宁可停止计算，也不要输出一个看似正常的错误金额。**

服务端记录有单次返回上限。返回不完整时，页面会清空表格和合计，并阻止打印、Excel 和 JSON 导出。不要为了“让页面显示出来”而随意降低校验或把截断当成成功。

---

## 2. 推荐的云服务器规格

最低建议：

- 1 核 CPU
- 2 GB 内存
- 20 GB 本地云硬盘
- Ubuntu 22.04/24.04、Debian 12 或同类 Linux
- 绑定一个域名
- 独立公网 IPv4，或云负载均衡器

生产建议：

- 2 核 CPU、4 GB 内存
- 程序盘和数据盘分开
- 数据盘使用 ext4 或 xfs
- 每天自动备份，另存一份到不同云账号、对象存储或另一台服务器
- 云安全组只放行 `80/tcp` 和 `443/tcp`

不要把 SQLite 数据目录放在 NFS、SMB、SSHFS、对象存储挂载、WSL 9p 或其他锁语义不可靠的网络文件系统上。SQLite 的 `fin.db`、`fin.db-wal`、`fin.db-shm` 必须放在本地文件系统或云硬盘上。

### 时区必须确认

账套期间缺省值来自服务器系统时间。云主机经常默认 UTC，在北京时间每月 1 日凌晨到早上 8 点之间，系统可能仍计算出上个月的默认账套期间。

```bash
sudo timedatectl set-timezone Asia/Shanghai
sudo timedatectl status
```

服务配置文件中也应保留：

```ini
TZ=Asia/Shanghai
```

账套期间是业务值，系统不会自动替你推进；时区只影响缺省月份，不能代替人工推进和结账。

---

## 3. 部署前准备

### 3.1 云厂商安全组

在云控制台配置入方向规则：

| 端口 | 协议 | 来源 | 用途 |
|---:|---|---|---|
| 22 | TCP | 你的办公出口 IP | SSH 管理；不要对全网开放 |
| 80 | TCP | `0.0.0.0/0` 或公司出口 | HTTPS 证书申请和 HTTP 跳转 |
| 443 | TCP | `0.0.0.0/0` 或公司出口 | 正式访问 |
| 8787 | TCP | 不开放 | 应用内部端口，只给 nginx/Caddy |

如果只允许公司固定公网 IP 访问，把 80/443 也限制为公司出口 IP。不要把 `192.168.0.0/16` 当成访问公网用户的来源地址；云服务器看到的是公司公网出口地址。

主机防火墙示例：

```bash
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow from YOUR_OFFICE_PUBLIC_IP to any port 22 proto tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable
sudo ufw status verbose
```

### 3.2 域名

把域名的 A 记录指向云服务器公网 IP。使用 IPv6 时再配置 AAAA 记录，并确认服务器和安全组确实支持 IPv6。域名解析生效后再申请证书：

```bash
dig +short fin.example.com
curl -I http://fin.example.com
```

### 3.3 上传并校验发布包

在开发机生成发布包后，把 `.tar.gz` 和校验文件上传到服务器：

```bash
sha256sum -c SHA256SUMS.txt

tar -xzf fin-console-1.1.0.tar.gz
cd fin-console-1.1.0
bash deploy/preflight.sh
```

`preflight.sh` 只读检查，不会修改系统。它会检查 Node、`node:sqlite`、时区、内存、swap、磁盘、文件系统、端口和反向代理。必须处理红色失败项；黄色提醒建议在上线前处理。

---

## 4. Linux + systemd 部署（推荐）

这是云服务器上的推荐方式。应用由 `finconsole` 非 root 用户运行，systemd 负责开机启动、崩溃重启和日志收集。

```bash
sudo bash deploy/install-linux.sh
```

安装脚本会：

1. 检查 Node.js 22.5+ 和 `node:sqlite`
2. 创建不能登录的 `finconsole` 系统用户
3. 程序安装到 `/opt/fin-console`
4. 数据保存到 `/var/lib/fin-console`
5. 构建前端并执行模块图和构建产物校验
6. 创建 `/etc/fin-console.env`，已有配置不会覆盖
7. 注册并启动 `fin-console.service`
8. 轮询 `/api/health`，确认服务真的启动

首次启动后查看管理员初始口令：

```bash
sudo journalctl -u fin-console -n 60 --no-pager
```

常用命令：

```bash
sudo systemctl status fin-console
sudo systemctl restart fin-console
sudo systemctl stop fin-console
sudo journalctl -u fin-console -f
sudo journalctl -u fin-console --since today
```

### 4.1 环境配置

配置文件：`/etc/fin-console.env`。权限必须是 `root:root 600`。

云服务器使用反向代理和 HTTPS 时，至少确认以下内容：

```ini
FIN_HOST=127.0.0.1
FIN_PORT=8787
FIN_DATA_DIR=/var/lib/fin-console
FIN_COOKIE_SECURE=1
FIN_TRUST_PROXY=1
FIN_BACKUP_INTERVAL_HOURS=24
FIN_BACKUP_KEEP=30
TZ=Asia/Shanghai
```

首次部署时 `FIN_ADMIN_PASSWORD` 可以留空，让系统生成一次性初始口令并写入日志。首次登录必须改密。改好后可以从环境文件中删除初始口令。

### 4.2 绑定数据盘

推荐把独立云硬盘挂载到 `/var/lib/fin-console`，再执行安装。示例流程中的设备名必须替换成云厂商实际设备：

```bash
lsblk
sudo mkfs.ext4 /dev/vdb
sudo mkdir -p /var/lib/fin-console
sudo mount /dev/vdb /var/lib/fin-console
sudo blkid /dev/vdb
sudo nano /etc/fstab
```

在 `/etc/fstab` 中使用 UUID：

```text
UUID=替换成实际UUID /var/lib/fin-console ext4 defaults,nofail 0 2
```

然后确认：

```bash
sudo mount -a
findmnt /var/lib/fin-console
```

不要在已有 `fin.db` 正在运行时直接卸载数据盘。

---

## 5. HTTPS 反向代理

应用应该保持 `FIN_HOST=127.0.0.1`，由 nginx 或 Caddy 对外提供 HTTPS。不要把 8787 直接暴露到公网。

### 5.1 Caddy（云上最简单）

Caddy 自动申请和续期 Let's Encrypt 证书，适合单台云服务器。

安装 Caddy 后：

```bash
sudo cp /opt/fin-console/server/deploy/Caddyfile /etc/caddy/Caddyfile
sudo nano /etc/caddy/Caddyfile
sudo caddy validate --config /etc/caddy/Caddyfile
sudo systemctl enable --now caddy
sudo systemctl reload caddy
```

把文件中的 `fin.example.com` 改成真实域名，并填写证书提醒邮箱。域名 A 记录必须已经生效，安全组必须放行 80 和 443。

证书生效后：

```bash
sudo nano /etc/fin-console.env
sudo systemctl restart fin-console
sudo systemctl reload caddy
curl -I https://fin.example.com
```

### 5.2 nginx + Certbot

使用包内模板：

```bash
sudo cp /opt/fin-console/server/deploy/nginx-fin-console.conf /etc/nginx/conf.d/fin-console.conf
sudo nano /etc/nginx/conf.d/fin-console.conf
sudo nginx -t
sudo systemctl reload nginx
sudo certbot --nginx -d fin.example.com
```

模板已包含：HTTP 跳转 HTTPS、TLS 1.2/1.3、HSTS、4 MB 请求体限制、健康检查、整套账导出超时和反向代理头配置。

### 5.3 X-Forwarded-For 注意事项

模板使用：

```nginx
proxy_set_header X-Forwarded-For $remote_addr;
```

不要随意换成 `$proxy_add_x_forwarded_for`。应用的 `clientIp()` 取 X-Forwarded-For 的第一个值，而 `$proxy_add_x_forwarded_for` 会保留客户端自行提交的值再追加真实地址，客户端可以伪造审计日志中的 IP。

如果 nginx 前面还有云负载均衡器，必须：

1. 只信任云厂商公布的负载均衡网段
2. 配置 nginx `set_real_ip_from`
3. 确认负载均衡器会覆盖而不是盲目追加客户端 XFF
4. 再把规范化后的 `$remote_addr` 传给应用

`FIN_TRUST_PROXY=1` 只能在应用端口不直接暴露、且代理链已明确可信时使用。

---

## 6. Docker 部署

适合没有 systemd 或希望用容器管理的云主机。数据保存在 Docker volume 中：

```bash
docker compose up -d --build
docker compose ps
docker compose logs -f fin-console
```

默认只把容器端口映射到宿主机回环：

```yaml
- "127.0.0.1:8787:8787"
```

前面仍然需要 Caddy 或 nginx。不要为了省事改成 `8787:8787` 并把 8787 加入公网安全组。

升级：

```bash
docker compose pull
docker compose up -d --build
```

升级前先导出整套账 JSON，并确认 Docker volume 仍然存在：

```bash
docker volume ls | grep fin-data
docker compose exec fin-console node scripts/backup-now.mjs
```

---

## 7. 首次登录和上线验收

上线后按顺序执行：

1. 用 HTTPS 域名登录
2. 修改管理员初始口令
3. 建立至少两个管理员账号
4. 建单位和项目受控清单
5. 设置编制单位名称
6. 确认账套期间和个人查看期间
7. 创建一个记账员和一个只读账号
8. 新增一条测试记录，确认保存和审计日志
9. 执行一次手工备份
10. 用另一台电脑或手机网络访问，确认 HTTPS 和登录正常

健康检查：

```bash
curl -s https://fin.example.com/api/health
```

匿名健康检查只返回 `ok` 和时间，不返回账套记录数。不要用 `?detail=1` 作为公网探针。

检查端口：

```bash
sudo ss -ltnp
```

正确状态通常是：nginx/Caddy 监听 80/443，Node 只监听 `127.0.0.1:8787`。

---

## 8. 备份、恢复和云端容灾

### 8.1 本机热备

服务默认每 24 小时备份一次，保留 30 份。也可手工执行：

```bash
cd /opt/fin-console/server
sudo -u finconsole node scripts/backup-now.mjs
ls -lh /var/lib/fin-console/backups/
```

热备通过 SQLite 在线备份接口生成，不需要停服务。不要直接在服务运行时复制 `fin.db`。

### 8.2 异地备份

同一块云硬盘上的 30 份备份不能抵抗账号误删、磁盘损坏、整机故障或勒索软件。至少每天把 `backups/` 同步到另一处：

- 云对象存储：OSS、COS、S3 等
- 另一云账号的存储桶
- 另一台不允许应用账号写入的备份服务器
- 云硬盘快照或跨可用区快照

示例流程：

```bash
sudo -u finconsole node /opt/fin-console/server/scripts/backup-now.mjs
# 再用云厂商 CLI 或 rclone 把 /var/lib/fin-console/backups/ 同步到异地
```

不要把访问密钥直接写进仓库、`package.json` 或 `/etc/fin-console.env`。使用云主机角色、短期凭证或权限最小化的专用账号。

### 8.3 恢复演练

备份没有经过恢复演练就不能算可靠。每月至少一次在临时目录或备用云主机上：

1. 解包同版本程序
2. 复制一份备份库
3. 启动临时实例
4. 登录并抽查模块、期间、审计和附件外部依赖
5. 记录恢复耗时和结果

直接替换数据库文件前必须停服，并同时处理 `fin.db-wal` 和 `fin.db-shm`。更推荐使用界面的 JSON 恢复：先预演，确认后输入 `替换全部数据` 执行。

---

## 9. 修改代码的目录地图

| 需求 | 修改位置 | 修改后 |
|---|---|---|
| 服务端字段、模块权威定义 | `lib/schema.js` | 重启服务，通常也要构建前端 |
| 数据读写、期间、结账、审计 | `lib/store.js` | 重启服务 |
| HTTP 接口和权限 | `lib/routes.js` | 重启服务 |
| 结转规则和漏录检查 | `lib/carry.js` | 重启服务并构建前端 |
| 数据库表、索引、迁移 | `lib/db.js` | 先备份，再重启验证 |
| 页面入口和布局 | `frontend/index.html` | `npm run build` |
| 页面样式 | `frontend/styles/app.css` | `npm run build` |
| 前端主流程 | `frontend/app/` | `npm run build` |
| 模块业务计算 | `frontend/app/modules/<key>.js` | `npm run build` |
| 通用模块外壳 | `frontend/app/modules/generic.js` | `npm run build` |
| 打印和 Excel | `frontend/app/print*.js`、`frontend/app/xlsspec.js` | `npm run build` |
| 安装和打包 | `deploy/`、`scripts/pack.mjs` | 重新打包 |

不要直接改 `public/`。`public/` 是构建产物，下次 `npm run build` 会被 `frontend/` 覆盖。

### 修改后的正确流程

```bash
# 1. 备份正式账套
sudo -u finconsole node scripts/backup-now.mjs

# 2. 在开发机修改源码并运行校验
npm run verify
npm run test:frontend
npm test
FIN_BROWSER=/path/to/chrome.sh npm run test:browser

# 3. 构建
npm run build

# 4. 生成发布包
node scripts/pack.mjs 1.1.1

# 5. 上传新包，在云服务器验 SHA256
sha256sum -c SHA256SUMS.txt

# 6. 服务器升级
sudo bash deploy/install-linux.sh
sudo systemctl status fin-console
```

### 前端改动和服务端改动的区别

- 只改 `frontend/`：构建后刷新页面即可；systemd 服务通常不必重启。
- 改 `lib/` 或 `server.js`：必须重启 `fin-console`。
- 改 `lib/db.js`：必须先备份，再重启观察数据库初始化和迁移日志。
- 改字段名：风险最高。字段名同时是数据库字段、前端 `r['字段名']` 取值键和导入导出契约，不能只改一处。

---

## 10. 新增模块的简易方法

推荐使用脚手架：

```bash
npm run new-module -- --example > cash.json
# 编辑 cash.json 后：
npm run new-module -- cash.json
npm run build
npm run verify
```

脚手架会生成或修改：

- `lib/schema.js` 中的服务端模块定义
- `frontend/app/modules/<key>.js`
- `frontend/app/modules/registry.js`
- 必要时替换 `index.html` 中的规划中占位页签

声明示例：

```json
{
  "key": "cash",
  "name": "资金管理",
  "entity": "资金记录",
  "periodField": "会计期间",
  "sortField": "会计期间",
  "fields": [
    { "name": "单位", "type": "text", "required": true },
    { "name": "会计期间", "type": "date", "required": true },
    { "name": "收款金额(元)", "type": "number" },
    { "name": "备注", "type": "text" }
  ]
}
```

### 新模块的四条规则

1. `key` 一旦有数据就不能改。
2. 字段中文名不能随意改，历史记录和前端取值依赖它。
3. 按月模块必须声明 `periodField`，常设台账使用 `null`。
4. 需要本月结转的模块，还要在 `lib/carry.js` 的 `CARRY_MODULES` 添加身份字段、照抄字段、输入字段和 `cumulative` 口径。

`cumulative: true` 适用于开累产值、工资年开累等累计值；`false` 适用于本期领用数量等当期发生数。两种口径不能混用。

### 链式计提模块的额外要求

如果模块按“本期累计 - 上期累计”计算，必须：

- 使用受控单位/项目名称
- 落库前执行名称规范化
- 测试首期、第二期、缺期、名称改名和重复结转
- 在数据不完整时拒绝计算
- 增加至少一个跨期回归测试

名称差一个空格或括号写法不同，就可能把同一条链拆成两条，导致本期金额被当成全部累计金额。不要用前端提示代替服务端校验。

---

## 11. 修改数据库和 API 的安全流程

数据库改动不能只在生产库上试。推荐流程：

```bash
# 开发机先复制一份正式备份到临时目录
cp fin.db /tmp/fin-test.db

# 修改 db.js 后运行完整测试
npm test

# 在干净目录应用补丁并测试
rm -rf /tmp/fin-checkout
# 解包发布包到 /tmp/fin-checkout
npm run verify
npm run test:all
```

API 修改至少要覆盖：成功响应、无权限响应、已结账响应、字段校验失败、并发版本冲突和异常输入。不要只测试浏览器按钮；任何用户都可以直接请求 API，服务端必须独立完成权限和数据校验。

---

## 12. 升级和回滚

### 升级

```bash
# 升级前
sudo -u finconsole node /opt/fin-console/server/scripts/backup-now.mjs
sudo systemctl status fin-console

# 上传并校验新版发布包
sha256sum -c SHA256SUMS.txt

# 在新版解包目录执行，脚本会保留数据和已有配置
sudo bash deploy/install-linux.sh
sudo systemctl status fin-console
curl -s http://127.0.0.1:8787/api/health
```

升级后检查：登录、总览、一个按月模块、一个常设台账、打印、Excel、审计日志和结账锁。

### 回滚

程序回滚和数据回滚要分开判断：

- 只有程序有问题：恢复旧程序包，保留当前数据
- 数据被错误写入：先停服，再使用升级前备份或 JSON 恢复
- 数据库结构已变更：必须使用对应版本的恢复流程，不要直接把新库文件塞给旧程序

```bash
sudo systemctl stop fin-console
# 替换程序目录或重新执行旧版本 install-linux.sh
sudo systemctl start fin-console
sudo journalctl -u fin-console -n 80 --no-pager
```

---

## 13. 常见故障

### 页面打不开

```bash
sudo systemctl status fin-console
curl -s http://127.0.0.1:8787/api/health
sudo ss -ltnp
sudo journalctl -u fin-console -n 100 --no-pager
```

本机健康但公网打不开：检查云安全组、UFW、域名解析、nginx/Caddy 状态和证书。

### 登录后马上掉线

确认 HTTPS 已生效、域名没有混用 IP 和域名，并检查：

```ini
FIN_COOKIE_SECURE=1
FIN_TRUST_PROXY=1
```

如果 HTTP 访问时提前打开 `FIN_COOKIE_SECURE=1`，浏览器不会通过明文 HTTP 发送 Cookie，表现就是无法保持登录。

### 默认月份不对

```bash
timedatectl
sudo timedatectl set-timezone Asia/Shanghai
sudo systemctl restart fin-console
```

同时确认顶栏的个人查看期间和全局账套期间。个人查看期间不会推进账套期间。

### 页面显示“数据未取全”

这是保护机制，不是普通空表。检查服务端返回的 `total`、`returned`、`truncated`，以及 `FIN_MAX_ROWS`。不要只把提示隐藏掉；应减少单模块异常数据量、检查接口过滤条件或合理提高服务端和前端上限。

### 打印列被别人改了

打印/Excel 列设置是共享设置，修改会影响所有账号。界面已明确提示“全账套共享”。这不是浏览器个人偏好。

### 忘记密码

```bash
cd /opt/fin-console/server
sudo -u finconsole node scripts/reset-password.mjs --list
sudo -u finconsole node scripts/reset-password.mjs admin
```

不要把新密码写入命令行参数或提交到 Git。

---

## 14. 发布前检查清单

### 代码发布

- [ ] `npm run verify` 通过
- [ ] `npm run test:frontend` 通过
- [ ] `npm test` 通过
- [ ] 有浏览器环境时 `npm run test:browser` 通过
- [ ] 新字段、新模块有回归测试
- [ ] 未直接修改 `public/`
- [ ] 未把 `config.json`、`data/`、`data-demo/` 打进发布包
- [ ] 已生成 SHA256 校验文件

### 云服务器

- [ ] Node.js 22.5+
- [ ] `node:sqlite` 可用
- [ ] 时区为 `Asia/Shanghai`
- [ ] 数据目录在本地云硬盘
- [ ] 数据盘已加入 `/etc/fstab`
- [ ] 已配置自动备份
- [ ] 已完成异地备份或快照
- [ ] 安全组未开放 8787
- [ ] 80/443 按需要开放
- [ ] HTTPS 证书有效并能自动续期
- [ ] `FIN_COOKIE_SECURE=1`
- [ ] `FIN_TRUST_PROXY=1`
- [ ] `/etc/fin-console.env` 权限为 600
- [ ] 已创建第二个管理员
- [ ] 已做一次恢复演练

---

## 15. 版本化发布

在开发机打包：

```bash
node scripts/pack.mjs 1.1.1
```

产物位于 `dist/`：

```text
fin-console-1.1.1.tar.gz
fin-console-1.1.1.zip
SHA256SUMS.txt
```

发布脚本会先重新构建前端，并排除：

- `data/`、`data-demo/`
- `config.json`
- `.git/`、日志、临时目录
- `node_modules/`
- `dist/`

Linux 使用 tar.gz，Windows Server 使用 zip。云服务器应优先使用 tar.gz，并在服务器上执行 `sha256sum -c SHA256SUMS.txt`。

---

## 16. 云端部署的责任边界

应用自身已经提供口令哈希、首登改密、登录限流、权限、审计、结账锁、备份和安全响应头；但以下内容仍由部署方负责：

- 云账号和 SSH 密钥安全
- 云安全组和主机防火墙
- HTTPS 证书和域名 DNS
- 异地备份和恢复演练
- 云硬盘快照、跨区域容灾
- 操作系统安全更新
- 对外网开放范围和 VPN/IP 白名单
- 正式财务系统中的复核、审批和入账

不要把“服务能打开”当成“云端部署完成”。完成部署的标准是：数据可恢复、端口收口、HTTPS 正常、日志可查、升级可回滚、业务人员知道账套期间和漏录提醒的含义。
